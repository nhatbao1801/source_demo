version: '3.7'

services:
  web:
    build: ./app
    command: python manage.py runserver 0.0.0.0:8009
    volumes:
      - ./app/:/usr/src/app
    ports:
      - 8009:8009
    expose:
      - 8009
    env_file: .env
