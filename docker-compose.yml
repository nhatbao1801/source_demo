version: '3.7'

services:
  web:
    build: ./app
    command: python manage.py runserver 0.0.0.0:${SERVICE_PORT}
    volumes:
      - ./app/:/usr/src/app
    ports:
      - "${SERVICE_PORT}:${SERVICE_PORT}"
    expose:
      - "${SERVICE_PORT}"
    env_file: .env
