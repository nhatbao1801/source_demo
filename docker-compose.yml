version: '3.7'

services:
  web:
    build: ./app
    command: python manage.py runserver 0.0.0.0:8010
    volumes:
      - ./app/:/usr/src/app
    ports:
      - 8010:8010
    expose:
      - 8010
    env_file: .env
